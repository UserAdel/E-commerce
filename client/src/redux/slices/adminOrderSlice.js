import { createAsyncThunk } from "@reduxjs/toolkit";

export const fetchAllOrders = createAsyncThunk("admin/fetchAllOrders", async (_) => {
    try {
        // Implementation goes here
    } catch (error) {
        // Error handling goes here
    }
});